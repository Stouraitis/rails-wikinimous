<h3>These are all the articles</h3>

<ul>
  <% @articles.each do |article| %>
    <li>
      <%= link_to article.title, article_path(article)%>
      <%= link_to article.content, article_path(article)%>

      <%= link_to 'edit', edit_article_path(article)%>
      <%= link_to 'remove', article_path(article), method: :delete, data: { confirm: 'Are you sure to delete?'} %>
    </li>
  <% end %>
</ul>

<ul>
  <li>
          <%= link_to 'new.html.erb', new_article_path %>
  </li>
</ul>>
